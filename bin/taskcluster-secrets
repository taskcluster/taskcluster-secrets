#!/usr/bin/env node

let load = require('../lib').default;

load(process.argv[2], {
  process: process.argv[2],
  profile: process.env.NODE_ENV,
}).catch(err => {
  console.log(err.stack);
  process.exit(1);
});
