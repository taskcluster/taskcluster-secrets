id: http://schemas.taskcluster.net/secrets/v1/get-signed-secret.json#
$schema:  http://json-schema.org/draft-04/schema#
title:        "A TaskCluster Secret"
description: |
  A request to read a signed TaskCluster Secret
type:         object
properties:
  signingKey:
    description: |
      A signing key (for secret verification).
    type:         string
additionalProperties: false
